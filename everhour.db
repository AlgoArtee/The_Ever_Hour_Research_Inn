CREATE TABLE equipment (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    quantity INTEGER NOT NULL,
    description TEXT
);

CREATE TABLE rooms (
    id INTEGER PRIMARY KEY,
    number INTEGER NOT NULL,
    has_lab BOOLEAN NOT NULL
);

CREATE TABLE room_equipment (
    id INTEGER PRIMARY KEY,
    room_id INTEGER NOT NULL,
    equipment_id INTEGER NOT NULL,
    FOREIGN KEY (room_id) REFERENCES rooms(id),
    FOREIGN KEY (equipment_id) REFERENCES equipment(id)
);


INSERT INTO equipment (name, quantity, description) VALUES
    ('Microscope', 2, 'A binocular microscope with a magnification range of 40x to 400x.'),
    ('Bunsen burner', 1, 'A gas burner used for heating and sterilizing.'),
    ('pH meter', 1, 'A device used for measuring the acidity or alkalinity of a solution.');

INSERT INTO rooms (number, has_lab) VALUES
    (101, true),
    (102, false),
    (103, true);

INSERT INTO room_equipment (room_id, equipment_id) VALUES
    (1, 1),
    (1, 2),
    (3, 1),
    (3, 3);